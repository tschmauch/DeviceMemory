import React from "react";
import '../index.css';

const Header = () => (
	<div className="Header">
		<h1>The Torture Memory Game</h1>
		<p>
			Click images to progress but don't click the same image twice!
    </p>
	</div>
);

export default Header;
